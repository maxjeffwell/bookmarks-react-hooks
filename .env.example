# Docker Compose Configuration
NODE_ENV=production
CLIENT_PORT=3000
SERVER_PORT=3001

# React App Configuration
REACT_APP_API_BASE_URL=/api

# Neon Database Configuration
# Get your connection string from: https://console.neon.tech/
DATABASE_URL=postgresql://user:password@host.neon.tech/database?sslmode=require

# JWT Authentication Configuration
# For local development only - production uses Doppler
# Generate secure secrets with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-jwt-secret-change-in-production
JWT_REFRESH_SECRET=your-jwt-refresh-secret-change-in-production

# =============================================================================
# DOPPLER SECRETS (Production)
# =============================================================================
# The following secrets should be configured in Doppler for production:
#
# BOOKMARKED_DATABASE_URL      - Neon PostgreSQL connection string
# BOOKMARKED_OPENAI_API_KEY    - OpenAI API key (for AI features)
# BOOKMARKED_JWT_SECRET        - JWT access token secret (64+ bytes hex)
# BOOKMARKED_JWT_REFRESH_SECRET - JWT refresh token secret (64+ bytes hex)
# CACHE_INVALIDATION_API_KEY   - API key for cache sync between deployments
#
# Doppler syncs to:
# - Vercel (via Doppler-Vercel integration)
# - Kubernetes (via External Secrets Operator)

# OpenAI Configuration for AI Features
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=0.3
OPENAI_MAX_TOKENS=500

# AI Feature Flags
AI_FEATURES_ENABLED=true
AI_CACHE_ENABLED=true

# Local AI Configuration
# Set to 'true' to use local OpenVINO AI instead of OpenAI (default: true in docker-compose)
USE_LOCAL_AI=true
# URL of the shared AI gateway (default: http://shared-ai-gateway:8002)
LOCAL_AI_URL=http://shared-ai-gateway:8002
